server:
  port: 4004

spring:
  application:
    name: api-gateway

  profiles:
    default: local

logging:
  level:
    org.springframework.cloud.gateway: TRACE

management:
  endpoints:
    web:
      exposure:
        include: "*"

  endpoint:
    gateway:
      enabled: true